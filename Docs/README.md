# Docs

- `RANGENET_SGSLAM_CONTRACT.md`: frozen `RangeNet -> .label -> SG-SLAM` interface, including raw-label and bounded-subset guidance.
- `AWS_GPU_BASELINE_RUNBOOK.md`: concrete AWS GPU setup, pretrained model download, minimal exporter dependencies, and `700`-scan/full-sequence export recipes.
- `RANGENET_PIPELINE_FOLDERS.md`: maps RangeNet++ paper steps (A-D) to `RangeNet/train` folders and key modules.
